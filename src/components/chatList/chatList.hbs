<!--Левая сторона общее-->
<ul class="chat-list">
    {{#each chats }}
        {{{
        ChatItem
                avatar=this.avatar
                id=this.id
                lastMessage=this.lastMessage
                title=this.title
                unreadCount=this.unreadCount
        }}}
    {{/each}}
</ul>

