<input
        class="chat-bottom__input"
        type={{ type }}
        name={{ name }}
        placeholder="{{ placeholder }}"
        ref="input"
    {{#if hidden}} hidden {{/if}}
        name="{{name}}"
>
