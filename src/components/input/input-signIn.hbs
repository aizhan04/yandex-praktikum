<input
                class="input__element"
                placeholder=""
                type="{{type}}"
                name="{{name}}"
                id="{{name}}"
                ref="input"
            {{#if hidden}} hidden {{/if}}
/>
