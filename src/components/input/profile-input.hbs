    <input
            ref="input"
            class="field-profile__input"
            type={{ type }}
            placeholder={{ placeholder }}
            name={{ name }}
        {{#if disabled }}
            disabled
        {{/if}}
    >
